/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
          {
            protocol: 'https',
            hostname: 'ze6i4xqr0l.ufs.sh',
          },
        ],
      },
};

export default nextConfig;
